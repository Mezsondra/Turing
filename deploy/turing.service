# Copy to /etc/systemd/system/turing.service, then:
#   systemctl daemon-reload && systemctl enable --now turing
[Unit]
Description=Turing Test Challenge
After=network.target

[Service]
Type=simple
User=turing
WorkingDirectory=/sdc/turing
Environment=NODE_ENV=production
Environment=DATABASE_PATH=/sdc/turing/turing.db
ExecStart=/usr/bin/env npm start
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
